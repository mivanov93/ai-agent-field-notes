---
layout: null
permalink: /search.json
sitemap: false
---
{%- assign docs = site.html_pages | where_exp: "p", "p.url != '/404.html'" -%}
[
{%- for p in docs -%}
{%- if forloop.first == false %},{% endif -%}
{"t":{{ p.title | default: "Untitled" | jsonify }},"u":{{ p.url | relative_url | jsonify }},"c":{{ p.content | strip_html | normalize_whitespace | strip | jsonify }}}
{%- endfor -%}
]
